# 依赖版本固定（2026-07-13）
# 原因：Streamlit Community Cloud 默认 Python 已升级到 3.14，
#       搭配最新原生库（streamlit 1.59 / pandas 3.0 / numpy 2.5 / pyarrow 25）
#       会在 streamlit 启动时触发 Segmentation fault（进程未加载 app.py 即崩溃）。
# 解决：将部署 Python 设为 3.12，并固定到以下经过验证的稳定版本组合。
# 部署步骤：Cloud 控制台 → Manage app → Settings → Python version 选 3.12 →
#          删除并重新部署（Python 版本只能在重新部署时修改）。

streamlit==1.45.1
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5
pdfplumber==0.11.4
pypdfium2==5.11.0
